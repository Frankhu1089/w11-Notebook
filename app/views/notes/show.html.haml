%h1=@note.title
%p=@note.content

=link_to "All Note",notes_path,class: "btn btn-default"
=link_to "Edte",edit_note_path(@note),class: "btn btn-default"
=link_to "Delete",note_path(@note), method: :delete, data: { confirm: "Are you sure?" },class: "btn btn-default"
